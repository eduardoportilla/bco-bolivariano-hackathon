{
  "name": "banco-bolivariano-hackathon",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "preview:web": "turbo preview:mfe --filter=web-*",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "clean": "node scripts/clean.js",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "reset:macos": "watchman watch-del-all && find . -name 'node_modules' -type d -prune -exec rm -rf {} + && rm -rf $TMPDIR/metro-* && rm -rf apps/mobile/ios/Pods apps/mobile/ios/Podfile.lock && rm -rf ~/Library/Developer/Xcode/DerivedData && pnpm install && cd apps/mobile/ios && pod install --repo-update"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.7.4",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.27.0",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "fb-watchman": "^2.0.2"
  }
}