---
description: Core coding standards for the BBH project
alwaysApply: true
---

# Core Standards

> **Rules vs Guidelines:** This file contains concise rules for quick reference. See `docs/guidelines/` for detailed documentation with examples.

## Tech Stack

- Web: React 19, TypeScript, Vite, TailwindCSS 4.1, shadcn/ui
- Mobile: React Native CLI, TypeScript
- HTTP: Axios (use instead of fetch)
- Forms: react-hook-form + zod (v4 API)
- State: Zustand (client), React Query (server)
- Dates: date-fns (use for date manipulation and formatting)
- Monorepo: pnpm, Turborepo

## Documentation and Patterns

- Use Context7 MCP server for up-to-date documentation
- Prefer official documentation patterns over outdated examples

## Critical Rules

- No emojis in code or comments
- Named exports only (no default exports)
- Explicit types (no implicit any)
- All imports must be included
- Use @repo/ prefix for internal packages
- Spanish for UI text, English for code
- No console.log (use logger from shared/utils)
- No hardcoded secrets
- Always write tests for new features
- Prefer async/await over .then() chains
- Use Error Boundaries for React error handling

## Code Quality

### Clean Code Principles

- Write self-documenting code with descriptive names
- Keep functions small and focused (single responsibility)
- Avoid deep nesting
- Extract complex conditions into named variables or functions
- DRY: Extract repeated logic into reusable functions/components

### Maintainability

- Prefer composition over inheritance
- Use early returns to reduce nesting
- Keep files under 500 lines (split if larger)
- Group related code together
- Add comments only for "why", not "what" (only when necessary)
- Use constants for magic numbers and strings (SCREAMING_SNAKE_CASE)

### TypeScript Patterns

- Use `interface` for object shapes
- Use `type` for unions, primitives, and utility types
- Explicit return types on exported functions
- Use `unknown` instead of `any` when type is uncertain
- Naming: PascalCase for types/interfaces, camelCase for variables/functions, SCREAMING_SNAKE_CASE for constants

### Component Best Practices

- One component per file
- Props interface above component definition
- Destructure props in function signature
- Use custom hooks to extract complex logic
- Memoize expensive computations (useMemo, useCallback)
- Avoid prop drilling (use context or state management)
- Include accessibility attributes (aria-label, role, etc.)

### File Naming

- Components: `PascalCase.tsx` (e.g., `LoginForm.tsx`)
- Hooks: `camelCase.ts` (e.g., `useAuth.ts`)
- Utilities: `camelCase.ts` (e.g., `formatters.ts`)
- Types: `*.types.ts` (e.g., `auth.types.ts`)
- Tests: `*.test.ts` or `*.test.tsx`

### Error Handling

- Always handle errors explicitly
- Use try/catch for async operations
- Provide meaningful error messages
- Never swallow errors silently

## Package Structure

| Package | Name | Purpose |
|---------|------|---------|
| packages/core | @repo/core | Shared business logic |
| packages/ui-web | @repo/ui | Web design system |
| packages/eslint-config | @repo/eslint-config | Shared linting |
| packages/typescript-config | @repo/typescript-config | TS configs |

## Environment Variables

- Web: Use `VITE_` prefix (e.g., `VITE_API_URL`)
- Mobile: Use `react-native-config` (no prefix, e.g., `API_URL`)
- Use `.env.example` as template (we never commit `.env` files)
- Access via `import.meta.env` (web) or `Config` (mobile)

## Commands

- `pnpm dev` - Start development
- `pnpm build` - Build all
- `pnpm typecheck` - Type check
- `pnpm lint` - Lint all
- `pnpm test` - Run tests
- `pnpm clean` - Reset project

## Import Order

1. React and external libraries
2. Internal packages (@repo/*)
3. Relative imports
4. Type-only imports
5. Styles

## Commits

Conventional commits in Spanish:

```
feat(web-accounts): agregar vista de movimientos
fix(mobile): corregir navegacion en dashboard
refactor(ui): simplificar componente Button
```

Scopes match folder names: core, ui, web-shell, web-accounts, mobile

## References

Detailed documentation in `docs/guidelines/`:

| Guideline | Content |
|-----------|---------|
| `coding.md` | TypeScript, imports, patterns |
| `components.md` | Component structure, props |
| `architecture.md` | Feature organization, core package |
| `services.md` | API, forms, state, error handling |
| `security.md` | OWASP, tokens, validation |
| `styling.md` | TailwindCSS, shadcn/ui |
| `mobile.md` | React Native patterns |
| `testing.md` | Vitest, Jest, Playwright |
