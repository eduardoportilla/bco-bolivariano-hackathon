/**
 * Shared mock data and handlers.
 *
 * Platform-specific exports:
 * - Web: import { worker } from '@repo/core/test/mocks/browser'
 * - Mobile: import { server } from '@repo/core/test/mocks/native'
 */
export { handlers, MOCK_USER, MOCK_ACCOUNTS, MOCK_TRANSACTIONS } from './handlers';
