# API Configuration
# Base URL for API requests. Default: /api
VITE_API_URL=/api

# Mock Mode
# Set to 'true' for standalone development without backend (MSW intercepts all API requests)
# Set to 'false' for real backend connection (requires logging in via web-shell first)
VITE_API_MOCK=true
